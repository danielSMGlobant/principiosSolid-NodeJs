export class Shape {
  constructor (public ejeX: number, public ejeY: number) {}
  getArea = (): number => {
    return 0
  }
}

// export abstract class Shape {
//   constructor(public ejeX: number, public ejeY: number) {}
//   abstract getArea(): number
// }
